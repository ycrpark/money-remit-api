server:
  port: 80

spring:
  devtools:
    livereload:
      enabled: true
  
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: test
    password: 
  
  h2:
    console:
      enabled: true

logging:
  level:
    root: info
    money.remit.api: debug
    org.springframework: info
